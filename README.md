# local-sms-gateway

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Create a `.env` file:
   ```env
   API_KEY=your-secret-key
   ```
3. Start the server:
   ```bash
   npm start
   ```
